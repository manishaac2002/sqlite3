// ----This is documents is for installation process
// SQLite3 installation process
// https://www.sqlite.org/download.html using this link, we can install sqlite3 for windows, it changes according to your
// OS check through it.

//----After installation
// 3 file will be downloaded and extract all the files and save in one new folder and put it in C drive.
// Reference video - https://www.youtube.com/watch?v=L3FwRRx6bqo&pp=ygUIc3FsaXRlMyA%3D

//----How to use this(using methods)
// We can work on command prompt and it is not easy to use instead we can use SQLite studio editor.
// To install that https://sqlitestudio.pl/ using this download
// Reference video - https://www.youtube.com/watch?v=DPgb7aa5bYs&t=232s&pp=ygUIc3FsaXRlMyA%3D


//----------here some specific code for creating a table and inserting---------

// ---Define the SQL statement to create a table
// const createTableQuery = `
//     CREATE TABLE IF NOT EXISTS users (
//         id INTEGER PRIMARY KEY,
//         name TEXT,
//         age INTEGER
//     )
// `;

//---checking the connection
// Execute the SQL statement to create the table
// try {
//     db.exec(createTableQuery);
//     console.log('Table created successfully.');
// } catch (error) {
//     console.error('Error creating table:', error.message);
// }


//---Define the SQL statement to insert data into the table
// const insertDataQuery = `
//     INSERT INTO USERS (id, name, age)
//     VALUES (?, ?, ?)
// `;

// ---Data to be inserted
// const data = ['1', 'mala', '23'];

//---Execute the SQL statement to insert data into the table and through error
// try {
//     db.prepare(insertDataQuery).run(data);
//     console.log('Data inserted successfully.');
// } catch (error) {
//     console.error('Error inserting data:', error.message);
// } 

// import database from "better-sqlite3";

// // Define a global variable to hold the database connection
// let db;

// const dbConnection = async () => {
//   const dbPath = '../../memory.db'

//   if (!dbPath || typeof dbPath !== 'string' || dbPath.trim() === '') {
//     console.error('Invalid or empty database path provided.');
//     process.exit(1);
//   } else {
//     try {
//       db = new database(dbPath);
//       console.log('🟢 Connected to the database successfully...!');
//     } catch (error) {
//       console.error('🔴 Error connecting to the database:', error.message);
//       // Handle the error accordingly, e.g., exiting the application
//       process.exit(1);
//     }
//     return db;
//   }
// }


// export default { dbConnection, db };
